<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No INTERNET: profiles, leaderboard, and speech stay on the device. -->
    <application
        android:name=".MatAventurasApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.MatAventuras"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".engine.Platformer2dActivity"
            android:exported="false"
            android:theme="@style/Theme.MatAventuras.Game" />

        <activity
            android:name=".engine.Kart3dActivity"
            android:exported="false"
            android:process=":engine3d"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.MatAventuras.Game" />
    </application>
</manifest>
